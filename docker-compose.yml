services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: botarbitragem
      POSTGRES_USER: botarbitragem
      POSTGRES_PASSWORD: botarbitragem
    ports: ["${POSTGRES_PORT:-5433}:5432"]
    volumes: [postgres_data:/var/lib/postgresql/data]
volumes:
  postgres_data:
